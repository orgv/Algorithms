Given a sorted  increasing sequence of integers.
Construct an algorithm that gets a sorted increasing sequence sortedArr and an integer Z, and returns the closest element to Z in the sequence.
In case there are more than one closest element, return any one of them.
